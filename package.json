{
    "scripts": {
        "frontend": "npm start --prefix Frontend/client",
        "backend": "cd backend && nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"npm run backend\" \"npm run frontend\"",
        "heroku-postbuild": "npm install --prefix Frontend/client && npm run build --prefix Frontend/client"
    },
    "engines": {
        "node": "^8.5.5"
    },
    "dependencies": {
        "react-scripts": "^5.0.1"
    }
}
