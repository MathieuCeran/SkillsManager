{
    "scripts": {
        "frontend": "cd Frontend/client && npm start",
        "backend": "cd backend && nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"npm run backend\" \"npm run frontend\"",
        "heroku-postbuild": "cd Frontend/client && npm install && npm run start"
    },
    "engines": {
        "node": "^8.5.5"
    },
    "dependencies": {
        "react-scripts": "^5.0.1"
    }
}
