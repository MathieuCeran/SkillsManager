{
    "scripts": {
        "build": "(cd Frontend/client && npm run build)",
        "start-dev": "(cd Frontend/client && npm run start) & (cd backend && npm run start)",
        "start-prod": "(cd Frontend/client && npm run start) & (cd backend && npm run start)"
    },
    "engines": {
        "node": "^8.5.5"
    },
    "dependencies": {
        "react-scripts": "^5.0.1"
    }
}
