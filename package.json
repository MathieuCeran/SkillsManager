{
    "scripts": {
        "build": "react-scripts build",
        "start-dev": "(cd Frontend && npm run start) & (cd backend && npm run start)",
        "start-prod": "(cd Frontend && npm run start) & (cd backend && npm run start)"
    },
    "engines": {
        "node": "^8.5.5"
    },
    "dependencies": {
        "react-scripts": "^5.0.1"
    }
}
